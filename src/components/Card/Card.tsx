import Link from "next/link";

export default function Card() {
  return (
    <div className="bg-white rounded-md w-64 p-5">
      <p className="font-bold text-gray-800 text-sm">
        ここに過去のクロール情報が入って、クリックするとサイトマップに行けたり、、、
      </p>
    </div>
  );
}
